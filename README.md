# Folvy App Staging

Placeholder de `app.folvy.app` mientras se construye Folvy V1 (objetivo: septiembre 2026).

Este proyecto se reemplazará por el código real Folvy V1 en Sprint 1.

Build: ninguno, HTML/CSS puro.
Deploy: Vercel automático en cada push a main.
